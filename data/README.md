# Data

`nurses.json` and `log.json` are the site's source of truth. They're read directly by the app, and updated either by hand or through the `/admin` page, which commits changes here via the GitHub API.

The entries currently in these files are placeholder sample data — replace them with your actual classmates once you're ready to go live.
